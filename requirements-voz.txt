# Dependências OPCIONAIS para voz no terminal (python -m jarvis voz).
# O HUD web NÃO precisa disto — usa a voz do próprio browser.
#
# Nota: em Linux o PyAudio pode exigir 'sudo apt install portaudio19-dev python3-pyaudio'.
SpeechRecognition>=3.10
pyttsx3>=2.90
PyAudio>=0.2.13
